# Core runtime dependencies for DecoVAE.
#
# Pin policy: lower-bound the minor version we have actually tested with
# (Python 3.12, CUDA 12.4). environment.yml ships a bit-exact frozen snapshot
# of the full conda env if you need full reproducibility.

# Deep learning
# Pin to the torch 2.6 ABI line. torchvision 0.22 / torchaudio 2.7 require
# torch 2.7 and segfault with `operator torchvision::nms does not exist` when
# imported against torch 2.6 (hit 2026-05-22 after monai 1.5 upgrade pulled
# torchvision 0.22 transitively into the deco_v15 env).
torch>=2.6,<2.7
torchvision>=0.21,<0.22
torchaudio>=2.6,<2.7

# MONAI + medical imaging
# 1.5.0 adds rectified_flow scheduler (consumed by train_UNET / train_CONTROLNET).
# Sticks to ==-pin for now to keep MaisiV2 patch in sync with upstream API.
monai==1.5.0
nibabel>=5.3
pydicom>=3.0
itk>=5.4

# Scientific stack
numpy>=1.26,<2.0
scipy>=1.15
pandas>=2.2
scikit-image>=0.25
scikit-learn>=1.7

# Visualization / metrics
matplotlib>=3.10
seaborn>=0.13
lpips>=0.1.4
pytorch-msssim>=1.0
torch-fidelity>=0.3

# Wavelet / inducer
pywavelets>=1.8
pytorch-wavelets>=1.3
einops>=0.8

# Logging / experiment tracking
wandb>=0.19
tqdm>=4.67
tensorboard>=2.19

# Utilities
PyYAML>=6.0
h5py>=3.14
filelock>=3.11
